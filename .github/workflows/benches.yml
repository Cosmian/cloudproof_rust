---
name: Benches on demand

on: workflow_dispatch

jobs:
  bench:
    uses: Cosmian/reusable_workflows/.github/workflows/cargo-bench.yml@findex_ffi_instance
    with:
      toolchain: stable
      force: true
