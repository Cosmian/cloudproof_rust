---
name: Cloudproof build

on:
  push:
    tags:
      - '*'
  pull_request:

jobs:
  cloudproof_rust:
    uses: Cosmian/reusable_workflows/.github/workflows/cloudproof.yml@develop
    with:
      project-name: cloudproof_rust
      toolchain: nightly-2023-03-20
      kms-version: 4.4.1
      findex-cloud-version: 0.1.0
      branch-java: v6.0.0
      branch-js: v9.0.0
      branch-flutter: integration_tests
      branch-python: v4.0.0
    secrets: inherit
